!#/bin/bash

# First install and enable sudo if you chose to enable root user during iso install with 
# apt install sudo  --then--  adduser (username) sudo

# Install gnome core -- remove wifi installer -- reboot

sudo apt update && sudo apt install gnome-core -y && sudo apt purge ifupdown -y && sudo shutdown -r now 

# Enter the NetworkManager .conf file with 
# sudo nano /etc/NetworkManager/NetworkManager.conf 

# Charge managed=false to managed=true  Then save and reboot again with
# sudo shutdown -r now



